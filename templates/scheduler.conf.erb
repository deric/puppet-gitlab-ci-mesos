#!/bin/bash

# could be a Zookeeper URL or ip address of a single master
MESOS_MASTER="<%= @mesos_master %>"

# GitLab CI URL
GITLAB_CI_URL="<%= @gitlab_ci_url %>"

# Version of GitLab CI API
GITLAB_CI_API="<%= @gitlab_ci_api %>"

# directory for clonning repositories and building
GITLAB_CI_DIR="<%= @gitlab_ci_dir %>"

# For next runner registration
GITLAB_CI_TOKEN="<%= @gitlab_ci_token %>"

# Public key to use to authenticate against GitLab
GITLAB_KEY="<%= @gitlab_key %>"

MESOS_NATIVE_LIBRARY="<%= @mesos_lib %>"

LOG_DIR="<%= @log_dir %>"